# WebMCP Compliance Checklist

Audit date: 2026-09-01. This document converts the current WebMCP and WebMCP Challenge requirements into implementation gates. Rows marked verified have evidence against the deployed origin, not only a local build.

| Requirement                      | Aether decision                                                                                                                                                                                                                                                                                                                                                                                                                                             | Build evidence required                                                                                                                                                                                                                                                                                                                                                                                                                                                    | State                                                                              |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| A working WebMCP-powered web app | Imperative tools expose semantic Aether actions directly from the workspace page.                                                                                                                                                                                                                                                                                                                                                                           | `document.modelContext.registerTool` is called from `src/platform/webmcp/registry.ts` with no wrapper library, and the live origin at https://webmcp-production-38e5.up.railway.app answers `/health` with `persistence: postgres`. Eight tools were read back from `document.modelContext.getTools()` in a browser on that origin on a committed architecture, and sixteen once a repair future was open — the growth observed live, not asserted.                        | Local verified                                                                     |
| ChatGPT Site Tools compatibility | Top-level imperative registration only; no declarative or iframe dependency.                                                                                                                                                                                                                                                                                                                                                                                | Recorded in `docs/WEBMCP_EVALS.md`: the in-app browser discovered the initial surface and called `get_decision_record`, receiving the incident, active branch, human guardrail, attributed notes and recent commands.                                                                                                                                                                                                                                                      | Local verified                                                                     |
| Chrome compatibility             | Flag-enabled local testing and deployed origin-trial testing use the same tool registry.                                                                                                                                                                                                                                                                                                                                                                    | The issued origin-trial token is configured on the deployed service and the live origin serves the `Origin-Trial` header alongside the isolation headers; the sixteen-tool surface is asserted by registry tests and read back from the live origin, where it grows eight to sixteen as a repair future is created and shrinks to eleven once one is committed. The token decodes to this exact origin with feature `WebMCP`, and its expiry is checked at server startup. | Verified, except confirming activation in a profile with the testing flag disabled |
| Human-agent cooperative UI       | Agent actions use the same command pipeline and visibly update the architecture interface.                                                                                                                                                                                                                                                                                                                                                                  | Every agent tool routes through the same `dispatch` as a human click and its result is reported to a live activity panel; a registry test drives the whole journey through the registered tools.                                                                                                                                                                                                                                                                           | Verified                                                                           |
| Explicit JSON Schema             | Every tool has a narrow schema, required fields, enums/ranges, and runtime validation.                                                                                                                                                                                                                                                                                                                                                                      | Every tool advertises a JSON Schema with `additionalProperties: false`, and all fourteen are asserted — a schema that accepts an unadvertised field fails. Zod validates at the boundary, and each schema's `required` list is held to what Zod actually refuses without, so the advertised contract and the enforced one cannot disagree.                                                                                                                                 | Local verified                                                                     |
| Shared, current state            | Tool execution reads current workspace/branch state; state determines the registered surface.                                                                                                                                                                                                                                                                                                                                                               | Registration is keyed to workspace state: eight tools on a committed architecture, thirteen once the model is editable, sixteen when a repair future also exists. Component and region enums come from the live derived graph, so an agent can address a component added moments earlier. Exact lists are asserted by registry tests.                                                                                                                                      | Verified                                                                           |
| Human control                    | Agent may request review but never approve. Merge is unavailable without a non-stale human approval and is reconfirmed at execution.                                                                                                                                                                                                                                                                                                                        | No approve or merge tool is registered, destructive removal by an agent is refused, approval requires every current scenario clean, and any edit invalidates it; covered by branch-engine tests.                                                                                                                                                                                                                                                                           | Verified                                                                           |
| Tool annotations                 | Read-only tools are marked. `get_decision_record` returns note bodies that an agent can write through `add_decision_note`, so both the write and the read carry `untrustedContentHint: true` rather than the write alone, which would launder untrusted text into a trusted result. Component names reaching a read are bounded by schema to 32 plain characters, and branch names never reach output because the engine replaces them with canonical ones. | Every mutating tool declares `readOnlyHint: false` and every tool returning agent-written text declares `untrustedContentHint: true`; all seven write tools are asserted individually, and the converse is asserted too — a tool marked trusted must carry no agent text, verified by writing an injection marker through every free-text path and finding it only in `get_decision_record`.                                                                               | Local verified                                                                     |
| Cancellation and lifecycle       | Registration abort signals and execution cancellation are honored.                                                                                                                                                                                                                                                                                                                                                                                          | Registration uses an `AbortController` signal, the execution signal is passed through to simulation, and unchanged capability state does not re-register.                                                                                                                                                                                                                                                                                                                  | Verified                                                                           |
| Origin isolation                 | No `document.domain`; production uses verified COOP/COEP isolation headers.                                                                                                                                                                                                                                                                                                                                                                                 | The deployed origin returns `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp`; confirmed by `curl -I` against the live URL.                                                                                                                                                                                                                                                                                                       | Verified                                                                           |
| Permissions boundary             | No cross-origin exposure by default; any exception uses explicit secure allowlists.                                                                                                                                                                                                                                                                                                                                                                         | The live origin returns `Permissions-Policy: tools=(self)` and the application registers no cross-origin frame or `exposedTo` exception.                                                                                                                                                                                                                                                                                                                                   | Verified                                                                           |
| Clear agent outputs              | Outputs are structured, minimal, and distinguish retryable from terminal errors.                                                                                                                                                                                                                                                                                                                                                                            | Every rejection names the failed fields and the values that would succeed, results stay within the 2,000-character budget the registry enforces, and no result can be truncated into invalid JSON.                                                                                                                                                                                                                                                                         | Verified                                                                           |
| Tool-selection quality           | No overlapping “god tool” surface; direct and ambiguous prompts have expected calls.                                                                                                                                                                                                                                                                                                                                                                        | Sixteen bounded tools with no approval or merge surface; expected calls for direct and open-ended prompts are recorded in `docs/WEBMCP_EVALS.md`.                                                                                                                                                                                                                                                                                                                          | Verified                                                                           |
| End-to-end reliability           | Canonical outage journey is evaluated in correct order, including a mid-chain failure.                                                                                                                                                                                                                                                                                                                                                                      | The canonical journey passes on the deployed origin for all three seeded systems — payment platform, ride-hailing dispatch and AI inference — and on a blank canvas built from a brief, including the blocked-approval path and its recovery, and is covered by an ordered registry test.                                                                                                                                                                                  | Verified                                                                           |
| Challenge submission             | Live URL, public <3-minute YouTube demo with audio, public source repo, and visible open-source license.                                                                                                                                                                                                                                                                                                                                                    | Live URL, public source repository, and MIT licence are in place and linked from `docs/SUBMISSION.md`.                                                                                                                                                                                                                                                                                                                                                                     | Outstanding: demo video and Devpost entry                                          |

## Acceptance gate

The project cannot claim WebMCP compliance or challenge readiness until every row has evidence. A successful frontend build alone does not satisfy any runtime, security, evaluation, or submission row.

## The one check that cannot be run from a development environment

Everything else here is verified against the deployed origin. This is not,
because it requires changing a browser flag, and it is the difference
between "the token is served" and "the token is what activates the API".

Takes about two minutes:

1. Open `chrome://flags/#enable-webmcp-testing` and set it to **Default**
   — not Enabled. Enabled turns the API on everywhere, which is exactly
   what masks the thing being tested. Relaunch Chrome.
2. Open the live app and run in the console:

   ```js
   typeof document.modelContext;
   ```

   Expected: `"object"`. The origin trial is doing the work; the flag is off.

3. Open any unrelated origin — `https://example.com` will do — and run the
   same line.

   Expected: `"undefined"`. If it returns `"object"` there, the flag is
   still Enabled and step 2 proved nothing.

4. Back on the live app, confirm the surface is real rather than a stub:

   ```js
   (await document.modelContext.getTools()).length;
   ```

   Expected: `5` on a fresh workspace, `12` once a repair future exists.

What this establishes that a served header does not: the `Origin-Trial`
response header is present and decodes correctly for this exact origin —
both already verified — but only a flag-disabled profile shows that Chrome
**accepted** it. The token decodes to
`https://webmcp-production-38e5.up.railway.app:443`, feature `WebMCP`, and
its expiry is checked at server startup, so a mismatch or an expiry shows
in the deploy logs rather than only as a silent absence here.

Position as of 2026-09-01: every implementation row is verified against the deployed origin rather than a local build. Two items remain, and neither is an implementation gap — confirming Chrome activation in a profile with `chrome://flags/#enable-webmcp-testing` set to Default, and producing the two external submission artifacts.
